"""Prefetch and shimcache tools placeholders."""


def analyze_prefetch(path: str) -> dict:
    return {"tool": "analyze_prefetch", "path": path}


def get_shimcache(path: str) -> dict:
    return {"tool": "get_shimcache", "path": path}
